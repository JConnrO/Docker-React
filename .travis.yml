sudo: required
language: generic 

services: 
    - docker
    
before_install:
    - docker build -t jconnro/docker-react -f Dockerfile.dev .

script:
     - docker run -e CI=true jconnro/docker-react npm run test -- --coverage

      